# Atlas

Atlas is a standalone Python program for building a structured literature catalog from PDFs.

## Status

Bootstrap phase.

## Development

Create a virtual environment, install editable, then run:

```bash
pip install -e .
atlas --help
